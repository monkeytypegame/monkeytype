import { createSignal } from "solid-js";

export const [getNotificationBubble, setNotificationBubble] =
  createSignal(false);
export const [getAccountButtonSpinner, setAccountButtonSpinner] =
  createSignal(false);
